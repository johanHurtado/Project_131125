<!doctype html>
<html lang="es" data-bs-theme="light">
<head>
  <meta charset="utf-8" />
  <title>Ubicaciones</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    /* Tamaño base un poco más pequeño */
    body {
      padding: 0;
      font-size: 0.9rem;
    }

    /* Contenedor más ancho y centrado */
    .page-wrapper {
      max-width: 1400px;
      margin: 0 auto;
    }

    /* Header local */
    header.page-header {
      margin-bottom: 1rem;
    }
    header.page-header h1 {
      font-size: 1.15rem;
    }
    header.page-header p {
      font-size: 0.82rem;
    }

    /* Botón compacto reutilizable */
    .btn-compact {
      padding: 0.3rem 0.7rem;
      font-size: 0.8rem;
      line-height: 1.2;
    }

    .table thead th {
      white-space: nowrap;
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: .03em;
      background-color: #f8f9fa;
    }

    .table {
      --bs-table-cell-padding-y: 0.4rem;
      --bs-table-cell-padding-x: 0.5rem;
    }

    .table td {
      vertical-align: middle;
      font-size: 0.85rem;
    }

    /* Card cuadrada y fina */
    .table-card {
      border-radius: 0.375rem;
      box-shadow: 0 0.1rem 0.35rem rgba(15, 23, 42, 0.05);
      border: 1px solid #dee2e6;
      overflow: hidden;
    }

    /* Efecto sutil al pasar el mouse */
    .table tbody tr:hover {
      background-color: #f9fafb;
    }

    /* Acciones centradas y compactas */
    .col-acciones {
      text-align: center;
      white-space: nowrap;
      width: 160px;
    }

    .col-acciones .btn {
      margin-bottom: 0.1rem;
    }
  </style>
</head>
<body class="bg-white d-flex flex-column min-vh-100">

  <!-- NAVBAR SUPERIOR (igual que en equipos.ejs) -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
    <div class="container">

      <!-- Logo + Nombre -->
      <a class="navbar-brand d-flex align-items-center gap-2" href="/">
        <img 
          src="/img/logo.png"
          alt="USTA"
          style="height:32px; width:auto;" 
          class="rounded"
        >
        <span>PDS-001</span>
      </a>

      <!-- Botón colapsable móvil -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <!-- Contenido navbar -->
      <div class="collapse navbar-collapse" id="mainNavbar">

        <!-- Ítems del menú -->
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="/equipos">Equipos</a></li>
          <li class="nav-item"><a class="nav-link active" aria-current="page" href="/ubicaciones">Ubicaciones</a></li>
          <li class="nav-item"><a class="nav-link" href="/responsables-custodios">Responsables</a></li>
          <li class="nav-item"><a class="nav-link" href="/personas-mantenimiento">Personas</a></li>
          <li class="nav-item"><a class="nav-link" href="/cargos">Cargos</a></li>
          <li class="nav-item"><a class="nav-link" href="/mantenimientos">Mantenimientos</a></li>
          <li class="nav-item"><a class="nav-link" href="/usuarios">Usuarios</a></li>
          <li class="nav-item"><a class="nav-link" href="/mi-perfil">Mi perfil</a></li>
        </ul>

        <!-- Usuario + Salir -->
        <% if (currentUser) { %>
          <span class="navbar-text me-3 text-muted small">
            Usuario: <strong><%= currentUser.username %></strong> |
            Rol: <strong><%= currentUser.role %></strong>
          </span>
          <form method="post" action="/logout" class="d-inline m-0">
            <button class="btn btn-outline-danger btn-sm">Salir</button>
          </form>
        <% } %>

      </div>
    </div>
  </nav>

  <!-- CONTENIDO PRINCIPAL -->
  <main class="flex-grow-1">
    <div class="container-fluid py-3 px-4">
      <div class="page-wrapper">

        <!-- Header local (título + descripción + botón Nueva) -->
        <header class="page-header d-flex justify-content-between align-items-center">
          <div>
            <h1 class="h4 m-0">Ubicaciones</h1>
            <p class="text-muted mb-0">
              Gestión de sedes, edificios, pisos y salas donde se encuentran los equipos.
            </p>
          </div>
          <div>
            <button id="btnNueva" class="btn btn-primary btn-compact">➕ Nueva ubicación</button>
          </div>
        </header>

        <!-- Tabla dentro de card sutil -->
        <div class="card table-card">
          <div class="table-responsive">
            <table class="table table-bordered table-hover align-middle mb-0">
              <thead class="table-light">
                <tr>
                  <th>#</th>
                  <th>Identificación</th>
                  <th>Sede</th>
                  <th>Edificio</th>
                  <th>Piso</th>
                  <th>Sala</th>
                  <th>Creado</th>
                  <th>Actualizado</th>
                  <th class="col-acciones">Acciones</th>
                </tr>
              </thead>
              <tbody id="tbody">
                <% if (ubicaciones && ubicaciones.length) { %>
                  <% ubicaciones.forEach(u => { %>
                    <tr>
                      <td><%= u.id %></td>
                      <td class="fw-semibold"><%= u.identificacion %></td>
                      <td><%= u.sede %></td>
                      <td><%= u.edificio %></td>
                      <td><%= u.piso %></td>
                      <td><%= u.sala %></td>
                      <td><%= u.createdAt ? new Date(u.createdAt).toLocaleString('es-CO', { timeZone:'America/Bogota' }) : '' %></td>
                      <td><%= u.updatedAt ? new Date(u.updatedAt).toLocaleString('es-CO', { timeZone:'America/Bogota' }) : '' %></td>
                      <td class="col-acciones">
                        <button class="btn btn-warning btn-compact me-1" data-act="edit" data-id="<%= u.id %>">Editar</button>
                        <button class="btn btn-danger btn-compact" data-act="del" data-id="<%= u.id %>">Eliminar</button>
                      </td>
                    </tr>
                  <% }) %>
                <% } else { %>
                  <tr><td colspan="9" class="text-center text-muted py-4">Sin registros</td></tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>

        <!-- Modal -->
        <div class="modal fade" id="modal" tabindex="-1" aria-hidden="true">
          <div class="modal-dialog">
            <form class="modal-content" id="form">
              <div class="modal-header">
                <h5 class="modal-title" id="title">Nueva ubicación</h5>
                <button class="btn-close" data-bs-dismiss="modal" type="button"></button>
              </div>
              <div class="modal-body">
                <input type="hidden" id="id">
                <div class="mb-3">
                  <label class="form-label">Identificación</label>
                  <input id="identificacion" class="form-control" required>
                </div>
                <div class="mb-3">
                  <label class="form-label">Sede</label>
                  <input id="sede" class="form-control" required>
                </div>
                <div class="mb-3">
                  <label class="form-label">Edificio</label>
                  <input id="edificio" class="form-control" required>
                </div>
                <div class="mb-3">
                  <label class="form-label">Piso</label>
                  <input id="piso" class="form-control" required>
                </div>
                <div class="mb-3">
                  <label class="form-label">Sala</label>
                  <input id="sala" class="form-control" required>
                </div>
              </div>
              <div class="modal-footer">
                <button class="btn btn-secondary btn-compact" data-bs-dismiss="modal" type="button">Cancelar</button>
                <button class="btn btn-primary btn-compact" type="submit">Guardar</button>
              </div>
            </form>
          </div>
        </div>

        <div class="toast-container position-fixed bottom-0 end-0 p-3" id="toastContainer"></div>

      </div><!-- /.page-wrapper -->
    </div><!-- /.container-fluid -->
  </main>

  <!-- FOOTER "FIJO" ABAJO -->
  <footer class="mt-auto py-2 border-top bg-light text-center text-body-secondary small">
    © <%= new Date().getFullYear() %> USTA – Módulo de gestión de inventario y registro de mantenimientos de equipos informáticos HSRT
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/ubicaciones.js"></script>
</body>
</html>
